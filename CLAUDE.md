# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository (`dev-term`, under the `oobdev` / Out-of-Band Development org) is currently empty — it contains only a `LICENSE` file and no source code, build configuration, or documentation yet.

There are no build, lint, or test commands to document, and no architecture to describe, because no code exists. Re-run `/init` (or otherwise update this file) once the project has real structure to capture.

## License

The project uses a custom "MIT NON-AI License" (`LICENSE`), copyright Out-of-Band Development. It is MIT-like but adds an explicit restriction: the software and its derivatives may not be used to train or improve machine learning / AI models, and may not be included in any ML training dataset.
