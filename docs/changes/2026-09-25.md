# 2026-09-25

## Migrated off Terminal.Gui's obsolete static `Application`/`TextView` APIs, elevated CS0618 to error

Terminal.Gui v2.5.0 marks its static `Application.*` surface and `TextView` `[Obsolete]`; both were
still in use across `DevTerm.Console`/`DevTerm.Console.Tests`. Migrated every call site to the
instance-based `IApplication` API, and migrated `TuiMode.cs`'s output pane from `TextView` to the
`Terminal.Gui.Editor` NuGet package's `Editor` class (`Editor.Text` is a direct `string` property
like the old `TextView.Text`; `Editor.CaretOffset`'s setter scrolls the viewport to keep the caret
visible — the correct, doc-confirmed replacement for `TextView.MoveEnd()`). With both migrations
done, `dotnet_diagnostic.CS0618.severity = error` is now set in `.editorconfig`, so a future obsolete-
API usage fails the build instead of accumulating as a warning. Verified via a full clean
`dotnet build` (0 `CS0618` warnings) and a full passing `TestCategory=Unit` run.

## Fixed a hanging USBTMC unit test

`UsbtmcTransportTests.WriteAsync_TwoConsecutiveValidZeroLengthMessages_ReturnsAnEmptyReplyWithoutThrowing`
hung indefinitely rather than failing. Root cause was the test's own technique, not the transport:
every other test in the file reads the reply via a blocking `PipeReader.ReadAsync`, but a genuinely
empty USBTMC reply never advances bytes into the pipe and never completes it — there's no signal path
from "confirmed empty" to an awaiting reader, so `ReadAsync` blocks forever. Fixed by asserting via
the non-blocking `PipeReader.TryRead` instead (see the new `CLAUDE.md` constraint on this), plus an
`Assert.HasCount(3, device.WrittenFrames)` check to actually prove the retry-once path consumed and
tag-validated the *second* queued empty header, not just that the first one alone produced nothing.
Production `UsbtmcTransport.ReadReply`/`WriteAsync` logic was traced through in full and confirmed
already correct.

## Reworked the build's warning policy: every warning is now a build-breaking error

`Directory.Build.props` now sets `TreatWarningsAsErrors` (previously an empty `WarningsAsErrors`, a
narrower per-ID list). The solution was already sitting at a clean 0-warning baseline, so this makes
that state enforced going forward instead of just currently-true — any new analyzer finding or
compiler warning now fails `dotnet build` rather than silently accumulating until someone notices and
hand-elevates it in `.editorconfig`. Also:

- Fixed the real suppression mechanism for the `AD0001` warnings StyleCop.Analyzers throws on a
  `record struct` declaration (`SA1201`'s member-order table has no entry for that syntax kind) and
  on `DevTerm.Transports.Tcp.Tests`'s `SocketExceptionStub` class layout (`SA1500`/`SA1502`/`SA1508`,
  all `NullReferenceException` in the same internal helper). An `.editorconfig`
  `dotnet_diagnostic.AD0001.severity` line does nothing — confirmed empirically (build-server
  shutdown + `obj`/`bin` deleted + clean rebuild still showed identical warnings) — because AD0001 is
  Roslyn's `NotConfigurable` diagnostic tag, which ignores `.editorconfig` severity overrides
  entirely. The working fix is `Directory.Build.props`'s `<NoWarn>$(NoWarn);AD0001</NoWarn>`, a
  compiler-option-level filter.
- Set `csharp_style_prefer_primary_constructors = false:silent` so `IDE0290` stops suggesting a
  primary constructor, without retroactively flagging the handful of files that already use one for
  removal.
- Suppressed `CA1859` (prefer concrete types for performance) and `IDE0021` (expression-bodied
  constructor) as not-a-fit for this codebase's conventions.

See `docs/coding-standards.md`'s "How this is enforced" and "A known StyleCop quirk" sections for the
updated detail. Verified via a full clean `dotnet build --no-incremental` (0 warnings, 0 errors) and
a full passing `TestCategory=Unit` run after each change.

## Fixed a real bug in `Session.OpenAsync` found during a pre-merge branch review

A prior CA2016/MA0040 "forward the CancellationToken parameter" cleanup pass had changed
`Session.OpenAsync`'s `Task.Run(() => PumpAsync(_readLoopCts.Token))` into
`Task.Run(() => PumpAsync(_readLoopCts.Token), cancellationToken)` — forwarding `OpenAsync`'s own
token into `Task.Run`'s scheduling-cancellation parameter, not the token actually passed to
`PumpAsync`. If that token were cancelled before the thread pool started the delegate, `Task.Run`
would yield an already-`Canceled` task without ever running `PumpAsync`: the read loop would silently
never start (no exception out of `OpenAsync`, since it doesn't await `_readLoopTask`), and a later
`StopAsync`'s `await _readLoopTask` would throw `TaskCanceledException` uncaught out of
`CloseAsync`/`DisposeAsync`. Fixed by passing `CancellationToken.None` explicitly, with a comment
explaining why — see the new `CLAUDE.md` constraint. Caught by a review pass across the whole
`dev/code-cleanup` branch before merging to `main`, not a live symptom; verified via a full clean
build and a full passing `TestCategory=Unit` run (including `DevTerm.Core.Tests.Sessions.SessionTests`).

## Added a `Hardware` test category, plus real-hardware Integration tests for three more Rigol profiles

Added `TestCategories.Hardware` (whitelisted in `TestCategoryStandardsTests`) and applied it across
every existing real-hardware test class, so `TestCategory=Hardware` alone selects the full
real-device suite regardless of transport. Added `tests/DevTerm.Console.Tests/RealHardwareUsbtmcTests.cs`
covering the Rigol DM3058E, DS1102E, and DG1062Z over USBTMC — same in-process construction pattern
(no DI container, no subprocess) as the existing `RealHardwareSerialTests`, with `TestContext`
logging of connection info and each sent/received value, `Assert.Inconclusive` preflight via a new
`RealDeviceReachability.IsUsbtmcDeviceAvailable` (disambiguating the DS1102E from a DG1022 sharing
its exact VID:PID via `SerialNumber`, per `BACKLOG.md`'s existing USBTMC entry), and identity plus
1-2 safe read-only queries per device. The `RawPresenter` used for terminatorless replies (previously
private to `RealHardwareSerialTests`) is now shared via `DevTerm.Test.Utilities`. New
`devterm.runsettings` parameters hold each device's VendorId/ProductId/SerialNumber/ExpectedIdnReply.

First real-hardware run (`docs/test/2026-09-25-15-02-44.md`) found two new, real issues, both left
for a deliberate follow-up rather than fixed blind: the DG1062Z's `*IDN?` bulk-IN reply fails USBTMC
header decoding (a 2-byte transfer where 12 bytes are required), and `RealHardwareSerialTests`'s
shared `RunAsync` helper misreads a multi-chunk serial reply (confirmed against the HP 34401A) because
it only consumes one `Session.Output` item per command. Both tracked in `BACKLOG.md`. The DM3058E's
previously-parked USBTMC bulk-IN stall (docs/changes/2026-09-23.md) did not reproduce — likely
resolved as a side effect of this session's `Session.OpenAsync`/`Task.Run` fix above. Everything else
(both Korads, both Tektronix scopes over TCP, the DS1102E, two of four K8055 boards, the Busylight)
passed with no regressions.

Later the same session, added a Rigol DG1022 test too (`*IDN?`/`OUTPut?`/`FREQuency?`) with
`RealUsbtmcDg1022SerialNumber` set to its real, previously-confirmed serial (`DG1D125306284`, see
`docs/test/2026-09-24-17-39-35.md`) to disambiguate it from the DS1102E sharing its exact VID:PID.
Running it against real hardware found the DG1022's previously-documented "stuck at the device/USB
level" zero-byte-reply behavior (docs/changes/2026-09-24.md) is intermittent rather than permanent:
`*IDN?` succeeded twice in a row with a real reply, but the very next query (`OUTPut?`) both times
hit the already-fixed empty-reply retry's terminal `IOException`. Updated `BACKLOG.md`'s existing
USBTMC entry rather than filing a new one — see `docs/test/2026-09-25-15-02-44.md`'s follow-up
section for the full transcript. No production code changed.

Later still, a re-run of the Rigol DG1062Z test (which had earlier failed in this same session's
results matrix with a USBTMC header-decode error on `*IDN?`) passed cleanly against real hardware —
`*IDN?`/`SOURce1:APPLy?`/`SOURce1:FREQuency?` all answered correctly. That framing failure is
therefore also intermittent, not permanent, matching the DG1022's pattern above. `devterm.runsettings`
now has the DG1062Z's real, confirmed serial number (`DG1ZA232603118`) and `*IDN?` reply substring
(`DG1062Z`). `BACKLOG.md`'s USBTMC entry updated accordingly. No production code changed.

With this, every device profile (both Korads, the HP/Agilent/Keysight 34401A, both Tektronix scopes,
and all four Rigol USBTMC instruments) has now been confirmed against real hardware via the automated
`RealHardware*Tests` suite — removed the "Remaining real-hardware verification opportunity" note from
`TODO.md`'s in-progress list accordingly. The two remaining open issues from this pass (the DG1062Z/
DG1022's intermittent USBTMC failures, and `RealHardwareSerialTests`'s multi-chunk-reply harness bug
on the HP 34401A) stay tracked in `BACKLOG.md`'s USBTMC entry rather than `TODO.md`, since neither is
being actively worked yet.

## USBTMC transport protocol-conformance rework; root-caused the DG1022 "no reply" failures

Branch `dev/usbtmc-fix`. The full write-up is in
[`docs/design/features/usbtmc-protocol-conformance.md`](../design/features/usbtmc-protocol-conformance.md). I reviewed
`DevTerm.Transports.Usbtmc` against `docs/protocols/usbtmc/` and fixed eleven problems.

**Read path:**
- Replies were cut short: EOM was ignored, and a short packet didn't end the transfer.
- The bulk-IN buffer (65548 bytes) wasn't a whole number of packets, so libusb overflowed on large replies. WinUSB instead carries the surplus into the next read.
- A declared TransferSize above 2³¹ wrapped negative.

**Recovery:**
- There was no abort or clear, so one timeout left every later query off by one.

**Control requests and writes:**
- REN_CONTROL sent wValue=0, which de-asserts REN. An earlier "spec nit" change had introduced this, and `usbtmc-transport.md` now marks it reverted.
- A zero-length DEV_DEP_MSG_OUT could be sent.

**Device lifetime and concurrency:**
- `SystemUsbtmcDevice.Open` leaked every device listed after the match.
- Nothing serialized I/O or close.
- Disposing twice threw.

**Query detection:**
- It missed several shapes of query.

New pieces:
- `UsbtmcCodec.BulkInBufferSize`
- `IUsbtmcDevice.MaxPacketSize`/`AbortBulkIn`/`AbortBulkOut`/`Clear`
- GET_CAPABILITIES-gated remote/local
- `UsbtmcDeviceQuirks`
- Options `ClearOnOpen` (off), `RemoteOnOpen`, `RequestDelayMs`

Unit tests: 39 in `DevTerm.Transports.Usbtmc.Tests`, up from 24. Two existing tests had encoded a
spec-violating assumption (EOM clear across physical reads of one transfer) and were corrected.

Bench tested against a real DG1022 (`docs/test/2026-09-25-18-03-06.md`). A raw probe showed the device
**never** answers a REQUEST_DEV_DEP_MSG_IN sent back-to-back with the query, reproducibly across 3 runs.
Any gap of about 1 ms or more fixes it. That is the real cause of the "intermittent" DG1022 failures `BACKLOG.md` tracked: the
open-time control requests happened to supply the gap for `*IDN?` only. There are two fixes:
- `UsbtmcDeviceQuirks` gives 0x1AB1:0x0588 a 20 ms delay.
- It also skips REN, because the DG1022 timed out GO_TO_LOCAL and libsigrok blacklists RL1 for this PID.

After both fixes, `RealHardwareUsbtmcTests.CliMode_AgainstRigolDg1022...` passed 3 of 3 runs, and 7 unpaced CLI queries all
answered. I removed the DG1022 entry from `BACKLOG.md` and added follow-ups:
- Re-verify the DS1102E/DG1062Z/DM3058E.
- DS1102E `:WAV:DATA?` framing quirks (from the literature).
- A/B test the open-time ClearHalt.
- A hex-parser crash on non-hex input, which is unrelated to USBTMC.

## Verified the USBTMC rework on a real DS1102E; added transport-level USBTMC hardware tests

DS1102E bench results (`docs/test/2026-09-25-18-03-06.md`, follow-up section):
- The existing `RealHardwareUsbtmcTests` test passed 3 of 3 runs.
- 0 ms command→read-request gaps work, so this unit doesn't need the DG1022's delay. It shares the VID:PID, so it keeps the harmless 20 ms, and `UsbtmcDeviceQuirks` now says so.
- `:WAV:DATA? CHAN1` declares an exact TransferSize (`#800000600` + 600 samples = 610) followed by 10 constant padding bytes. The rework drops the padding and surfaces exactly the 610-byte block. That contradicts pyvisa-py #472's "TransferSize 10 bytes short" reading.

Added `DevTerm.Console.Tests.RealHardwareUsbtmcTransportTests`, 9 tests. They drive `UsbtmcTransport` directly and read raw bytes off `Input`.

Coverage:
- Per Rigol model: unpaced back-to-back queries (two passes) and close/reopen ×3 on one transport instance.
- DS1102E only: a waveform read checked byte-exact against its IEEE block length, then a clean `*IDN?`.

Categories and settings:
- Categories match the existing real-hardware pattern: `Integration`/`Usbtmc`/`Scpi` on the class, `Hardware` plus the model (`Rigol_Dg1022`, `Rigol_Ds1102e`, `Rigol_Dg1062z`, `Rigol_Dm3058e`) on each method.
- They use the same `RealUsbtmc*` runsettings parameters and are Inconclusive when a device isn't attached.
- This class and the existing `RealHardwareUsbtmcTests` are now `[DoNotParallelize]`. Both assemblies use method-level parallelism, and two tests claiming the same physical USB interface at once would fail on the second claim. They live in the same assembly for the same reason.

DS1102E: all 4 pass. The DG1022, DG1062Z and DM3058E ones have not been run against hardware yet (Inconclusive). They were written after the DG1022 had been swapped out, so the DG1022's results above come from the older Console-level test and the raw probe, not from these.

## All four Rigols verified; root-caused the DG1062Z "2-byte first read" (data-toggle desync)

With all four bench Rigols attached, the first run of the 13 USBTMC hardware tests failed both new DG1062Z tests.
A bulk-IN read came back as 2 bytes, too short for a header. The raw probe showed those 2 bytes (`\n+`) were the
*tail* of the device's 66-byte `*IDN?` transfer: its first 64-byte packet was lost, on the first query after open only.

**Cause:** `SystemUsbtmcDevice.Open`'s unconditional CLEAR_FEATURE(ENDPOINT_HALT) on both bulk endpoints.
- It resets the host's data toggle (USB 2.0 §9.4.5).
- The DG1062Z doesn't reset its own toggle to match, so the host drops the device's next packet as a retransmission.
- A/B test over 12 opens: broken on 12 of 12 with the clear, intact on 12 of 12 without it.

**Fix:** a new `ClearHaltOnOpen` option, default off, pinned by a unit test together with `ClearOnOpen`. A real STALL is
still cleared and retried per transfer, which covers the DM3000 first-write STALL the open-time clear was originally
added for. This resolves the BACKLOG DG1062Z entry. It also resolves the "A/B test the open-time ClearHalt" and
"re-verify DG1062Z/DM3058E" entries, which were removed.

After the fix, all 13 USBTMC hardware tests pass on all four devices across three runs. `devterm.runsettings` now has
the DM3058E's real serial (`DM3R232301438`) and `*IDN?` substring (`DM3058E`), and its stale "left blank" comments are
fixed. Details are in `docs/test/2026-09-25-18-03-06.md`, follow-up 2.

## Detected serial port descriptions on Linux and macOS

The Linux/macOS half of the Connection Editor's long/short serial port name (the Windows half landed
2026-09-18, see `docs/changes/2026-09-18.md`) — the last item on the Connection Editor's requested
list, removed from `BACKLOG.md` and the spec's Open items. The "Detected ports" picker (WPF dropdown,
TUI "Detect..." list) now shows e.g. `/dev/ttyUSB0 — FTDI FT232R USB UART (0403:6001, serial
A50285BI)`; picking one still writes only `/dev/ttyUSB0`, and the profile format is unchanged.

- **Shape**: nothing outside `DevTerm.Transports.Serial` changed. `SystemSerialPortDiscovery.GetPortDescriptions()`
  now picks by `OperatingSystem.IsWindows()`/`IsLinux()`/`IsMacOS()` (anything else still reports no
  descriptions). Keys are the full paths `SerialPort.GetPortNames()` reports on those OSes
  (`/dev/ttyUSB0`, `/dev/cu.usbserial-…`), so `SerialPortOption.From`'s lookup matches unchanged.
  Linux and macOS share one format (`SerialPortDescriptionFormat`): `<manufacturer> <product>
  (<vid>:<pid>, serial <serial>)`, the manufacturer dropped when the product already starts with it,
  `USB device <vid>:<pid>` when the device reports no strings, null when there's nothing at all. The
  serial number is included (unlike Windows' friendly name) because it's what tells two identical
  adapters apart.
- **Linux (`LinuxSerialPortDescriptions`) reads sysfs directly** — the same USB string descriptors
  udev's `ID_VENDOR`/`ID_MODEL`/`ID_SERIAL_SHORT` and `/dev/serial/by-id/` names come from, with no
  libudev dependency. For every `/sys/class/tty/<name>` it resolves `device` with a physical realpath
  (built on a single-level `readlink`; relative targets are resolved against the directory the link
  really lives in, since `/sys/class/tty/<name>` is itself a link; bounded at 40 hops like the
  kernel's ELOOP), then walks up at most 12 levels, staying under `/sys`, to the nearest directory
  with an `idVendor` file — the USB device, not the interface (`1-2:1.0`) or an upstream hub/root hub
  — and reads `manufacturer`/`product`/`serial`/`idVendor`/`idProduct`. Covers both usb-serial
  (`ttyUSB*`: `device` → the port dir, two levels below the device) and `cdc_acm` (`ttyACM*`:
  `device` → the interface, one level below). Non-USB ports (`ttyS*`, platform UARTs), ttys with no
  `device` link, and unreadable files get no entry. `/dev/serial/by-id/` was not used — it's derived
  from the same attributes and adds nothing sysfs doesn't already give.
- **macOS (`MacSerialPortDescriptions`) shells out to `ioreg`** rather than P/Invoking
  IOKit/CoreFoundation: `/usr/sbin/ioreg -a -l -r -c IOUSBHostDevice` (every USB device's subtree as
  an XML plist), parsed with `System.Xml.Linq` (DTD processing ignored — the plist has a
  `<!DOCTYPE>` the default reader rejects). Each `IOSerialBSDClient`'s `IOCalloutDevice` and
  `IODialinDevice` (`cu.*` and `tty.*` — `GetPortNames()` lists both) is mapped to its nearest
  `IOUSBHostDevice`/`IOUSBDevice` ancestor's `USB Vendor Name`/`USB Product Name`/`USB Serial Number`
  (falling back to `kUSBVendorString`/`kUSBProductString`/`kUSBSerialNumberString`) and
  `idVendor`/`idProduct`. The intermediate `IOUSBHostInterface` also carries `idVendor`/`idProduct`
  but not the strings, so only the device node counts as the ancestor. The process run is bounded:
  3 s, both pipes drained concurrently, killed (whole tree) on timeout; a missing `ioreg`, non-zero
  exit, timeout, or unparseable output all mean no descriptions, never an exception or a hang.
- **Tests (+38 in `DevTerm.Transports.Serial.Tests`, 19 → 57)**: `LinuxSerialPortDescriptionsTests`
  (11, Unit) builds a fake sysfs tree in a temp dir laid out like the real one (FTDI `ttyUSB0`,
  Raspberry Pi Pico `ttyACM0`, CP2102 behind a hub, a string-less CH340, `ttyS0` under `pnp0`, a
  virtual `tty`, a symlink loop, a missing root) — the symlinks are supplied as data through the
  injected `readlink` (creating real symlinks on Windows needs a privilege tests can't count on), and
  sysfs's `:` separators are written as `_` since NTFS forbids `:` in names.
  `MacSerialPortDescriptionsTests` (10 incl. data rows, Unit) parses a sample `ioreg -a` plist whose
  FTDI entry's property names/values (`idVendor` 1027, `idProduct` 24577, `IOCalloutDevice`
  `/dev/cu.usbserial-AE01EB7D`, `IODialinDevice`, `IOTTYDevice`) follow ioreg output posted in
  pololu/libusbp issue #8, plus a CDC-ACM Pico, a non-serial USB receiver, the `kUSB*String`
  fallback, a Bluetooth-style port with no USB parent, and malformed output.
  `SerialPortDescriptionFormatTests` (13 rows, Unit). `MacSerialPortDescriptionsProcessTests` (4,
  **Integration** — real processes) drives the bounded runner with this OS's stand-ins: missing
  executable, stdout returned, non-zero exit, and a 60 s `ping`/`sleep` killed at 500 ms. Full
  `dotnet build` 0 warnings; `dotnet test --filter TestCategory=Unit` all passing.
- **Not verified on a real Linux or macOS machine.** This environment is Windows only; both
  implementations are exercised solely through the unit tests' sample sysfs trees and sample `ioreg`
  output, and the macOS sample is a trimmed, hand-assembled plist rather than a capture. Worth
  checking on real machines: that .NET's `FileInfo.LinkTarget` returns sysfs's relative link text as
  assumed, `ioreg -a -l -r -c IOUSBHostDevice`'s actual nesting on current macOS (Apple Silicon /
  DriverKit `AppleUSBFTDI`, `AppleUSBACMData`), and the picker's appearance with a real adapter.

## Front ends never crash on errors; the 2026-09-23 TODO bugs; presenter lifetime; USBTMC DevicePath

Branch `dev/error-handling-and-todo`. I reviewed the CLI, TUI and WPF for crash/exit paths against the
rule "show the error, disconnect, let the user resume; reject invalid input". What changed:

**Core: `Session.Disconnected`.**
- The read loop used to die silently on a read failure (an unplugged cable, a reset socket, a
  presenter throwing), and its exception resurfaced from `CloseAsync`/`DisposeAsync`, crashing on close.
- A peer hang-up also ended the loop with no notice.
- Now the session closes itself on a read failure, a peer close, or a failed send, and raises
  `Disconnected` once. For a failed send it does this before the send rethrows.
- `CloseAsync` never throws for a transport-close failure. Open/close/fault are serialized, and each
  fault carries a generation so a stale fault can't hit a newer connection.
- 7 new `SessionTests`.

**Shared (`DevTerm.Configuration`).**
- `TypedInput.TryEncode` rejects input the parser can't encode with a message.
  - This was the CLI crash: non-hex text with `--parser hex` threw an unhandled `FormatException`.
  - A decimal value over 255 is rejected the same way.
- `ConnectionErrorMessages.ForDisconnect` gives the CTS/`--handshake` hint only for a serial timeout.
  Before, a USBTMC timeout told the user to check hardware flow control.

**CLI.**
- Invalid lines are rejected and the connection is left alone.
- A lost connection is reported once, and the next line reconnects first, then sends. A failed
  reconnect is reported and the next line retries.
- Only a failed first connect still exits (code 1).
- 4 tests drive the real loop through scripted stdin. A real capture (a local TCP stand-in hangs up,
  then the next line reconnects and gets a reply) is in `docs/user-guide/sending-and-receiving.md`.

**TUI.**
- A failed startup connect opens the TUI disconnected with the error (it used to exit to the shell).
- `Disconnected` flips the menu and send field, and is re-subscribed on a profile switch.
- Every menu action is guarded, with an error dialog instead of an exception escaping `Application.Run`,
  which only swallows it in RELEASE builds.
- Every fire-and-forget task (connect, send, switch, SCPI auto-detect) is observed.
- 4 new tests.

**WPF.**
- A failed startup connect, reconnect or profile switch is shown in the output list and the window
  stays open, disconnected. It used to show a modal and close the whole app.
- `Disconnected` handling and observed fire-and-forget tasks match the TUI.
- 4 new tests.

**Control panels (both).** Every command goes through one helper, so a rejected value or a device
failure is shown instead of vanishing as an unobserved task: an error dialog in the TUI (the modal
panel hides the main output), the red status line in WPF. There's a WPF test for a failing command.

**`TODO.md`'s 2026-09-23 bugs.**
- **Busylight custom color not persisting**:
  - WPF kept the last color in a per-window dictionary, lost on reopening the panel.
  - The TUI always started the picker at white.
  - Both now use the shared, in-process `LastPickedColors`, keyed by the button id.
- **Send history duplicates**: `SendHistory` now ignores a line identical to the one just before it,
  like shell history.
- **Window title missing the profile name**: a real cause was found and fixed.
  - The title matches the running options against saved profiles exactly, but the Connection Editor's
    `BuildOptions` rebuilt options from the form alone.
  - It reset every setting the form doesn't show (`Dtr`, `Rts`, the timeouts, `AsciiMaxLineLength`,
    `ManifestName`) to defaults and re-ordered presenters, so Load then Connect no longer matched the
    saved profile. It also silently changed the connection: a DTR-off profile connected with DTR on.
  - Now those settings carry over, and the presenter order is kept unless the selection was edited.
  - A test proves Load then Connect round-trips and the title finds the name.
  - Not re-checked on the machine the bug was reported from: its profiles weren't available here.
    That re-check stays in `TODO.md`.
- **SCPI Custom Command (Enter/Send threw)**: re-tested end to end in both front ends, through a real
  `ScpiControlSurface` + `Session`. Committing the field doesn't error or send, and Send puts exactly
  the typed command on the wire. It stays fixed (by 2026-09-23's change); the new tests keep it that way.

**Backlog items.**
- **Presenter lifetime**: every `IPresenter` and `PresenterCatalog` registration is now transient, one
  catalog per session, so two sessions can't share a stateful presenter. There's a test.
- **Real-hardware test harness**: `RealHardwareSerialTests`/`RealHardwareUsbtmcTests` took only the
  first `Session.Output` chunk per reply (an HP 34401A's `*IDN?` logged as `H` and still passed). The new
  `ReplyCollector` assembles chunks until a 300 ms quiet gap. It's unit-tested; **not yet re-run against
  hardware**, because nothing was attached when it landed.
- **USBTMC `DevicePath`**: discovery reported `null`. It's now the physical USB location,
  `usb:{bus}-{port chain}` from libusb's bus/port numbers (`UsbtmcDeviceLocation`). It's shown by
  `--listusbtmcdevices` (`at usb:…`), filled in by the Connection Editor's USBTMC picker, and used by the
  transport only when `SerialNumber` is blank, with no "first match" fall-back since the DS1102E and
  DG1022 share a VID:PID. It's unit-tested for formatting; **no instrument was attached to see a real
  location**.
- **WPF "not found" hints**: the Device Profiles window now shows the TUI's "(not found)" hint for the
  serial port and USB device rows, bound to `ConnectedDeviceNotFound`. There's a test.
- **Linux/macOS serial port descriptions**: merged from a parallel branch; see that section above.

The `DS1102E packet-boundary` item stays in `BACKLOG.md`, as asked. All 17 unit-test projects pass.

## Busylight custom color: picker buttons cut off; hex swatch next to Custom...

These were reported from real use of the Busylight panel.

**OK/Cancel cut off in the WPF picker.** `ColorPickerWindow` was a fixed 360 px tall with
`ResizeMode="NoResize"`, and its OK/Cancel row was docked *last* in the `DockPanel`. The content (swatch,
hex, the RGB and HSV groups, the buttons and the title bar) needs roughly 400 px at 100% scaling, so the
button row got whatever height was left and was clipped. The window couldn't be resized to reach it.
Now:
- The window opens sized to its content (`SizeToContent="Height"`) and is resizable
  (`CanResizeWithGrip`, `MinHeight` 200).
- The controls sit in a `ScrollViewer`.
- The button row is docked first, so it's always reserved.

`ColorPickerWindowTests` checks that OK/Cancel are fully inside the client area at the natural size, and
that when the window is shrunk to `MinHeight` OK is still visible while the controls scroll. The TUI
picker's layout already fit, so it's unchanged.

**Swatch next to Custom...** Both front ends now show a box beside every color-picker button, holding
the current custom color's hex value (`#FF6600`) on that color as its background. The text is black or
white by luminance (`LastPickedColors.UseDarkText`). The box is hidden until a custom color has been
set, including one picked in an earlier opening of the panel.
- WPF uses a `Border`.
- The TUI uses a `Label` with a true-RGB Terminal.Gui `Scheme`
  (`View.SetScheme(new Scheme(new Attribute(fg, bg)))`, checked against the installed v2.5.0 API).

Tests cover it (`ControlPanelWindowTests`, `ControlPanelModeTests`, `LastPickedColorsTests`). There are
new real captures: `wpf-`/`tui-control-panel-busylight-custom-color.png` and `wpf-color-picker.png`, in
`docs/user-guide/device-control-panels.md`. `LastPickedColors.Forget` lets the screenshot tests start
from a known state, since the store is process-wide. The remaining parts of the 2026-09-23 custom-color
UX note (a "Custom" radio option, Enter acting as Apply) stay in `BACKLOG.md`.
## Added dedicated `RealHardwareTcpTests`; fixed a global MSTest timeout bug and the HP 34401A's truncated-reply bug

Added `tests/DevTerm.Console.Tests/RealHardwareTcpTests.cs`, the TCP counterpart to the existing
`RealHardwareSerialTests`/`RealHardwareUsbtmcTests` — same in-process pattern, covering both
Tektronix 2230 oscilloscopes (`ID?`, pre-SCPI) and the Tektronix TDS2024 (`*IDN?`, SCPI) directly
over a real `TcpTransport`, instead of only the shallower identity-only check
`RealHardwareCliTests` already did across all three. Uses `AsciiPresenter` with each device
profile's own terminator (LF for the TDS2024, CR for the 2230s), not the shared terminatorless
`RawPresenter`, for the same reason described below.

Running it against real hardware surfaced two real, previously-latent bugs:

- **`devterm.runsettings`'s global `<MSTest><TestTimeout>5000</TestTimeout>` silently overrode any
  per-step `WaitAsync` timeout written into a test body.** The TDS2024 test answered `*IDN?`/`CH1?`
  correctly, then was killed mid-flight on its third query with "timed out after 5000ms" — not a
  device fault or the test's own 15s per-step timeout, but MSTest's 5s *whole-method* deadline,
  never hit before because no existing real-hardware test happened to chain enough real round-trips
  to exceed it. Fixed by raising `<TestTimeout>` to `60000` (60s), with a comment explaining why.
- **Every `TRIGger:...?` query tried against this TDS2024 unit hangs (never replies), while every
  other query answers normally.** Confirmed with both `TRIGger:MAIn:FREQuency?` and the cheaper
  `TRIGger:STATE?` (ruling out "expensive measurement" as the cause); swapping to `CH2?` (still the
  3rd command in the sequence) answered immediately, ruling out a "3rd command" positional issue
  too. Root cause not identified — the profile's own `Name` field notes this unit is "NOT the
  TDS2024B", which may be related, but that's unconfirmed. The test now uses `CH1?`/`CH2?` instead
  of any `TRIGger` query; the real device-level hang is tracked as a new `BACKLOG.md` item
  ("Tektronix TDS2024") rather than investigated further, since no packet-capture tooling is
  available in this environment.

Also fixed `RealHardwareSerialTests`'s previously-tracked `BACKLOG.md` "Tooling" entry: its shared
`RunAsync` used the terminatorless `RawPresenter` for all three serial devices, which only reads one
`Session.Output` item per command — correct for the Korads (no line terminator at all, so
`AsciiPresenter` would never flush) but wrong for the HP 34401A, whose real LF-terminated replies can
arrive over serial in multiple chunks, each firing `Session.Output` separately. Confirmed live: the
test previously logged `Received: H` (a single truncated character) instead of the real reply.
`RunAsync` now takes a presenter factory per test — the HP 34401A test passes `AsciiPresenter`, the
Korad tests keep `RawPresenter` — and a real-hardware re-run now shows the full replies (`*IDN?` ->
`HEWLETT-PACKARD,34401A,0,5-1-1`, `MEAS:VOLT:DC?` -> a real voltage reading), with the Korad tests
unaffected. See `docs/test/2026-09-25-18-57-22.md` for the full session transcript and results
matrix, including the bench's one-spare-cable Tektronix 2230 topology (only one of Host1/Host2 is
ever reachable at a time — expected, not a bug).

## Merged main: two fixes for the HP 34401A truncated reply, kept together

`main`'s `RealHardwareTcpTests` change (above) fixed the HP 34401A "Received: H" problem by giving
`RealHardwareSerialTests.RunAsync` a presenter per device: `AsciiPresenter` for the line-terminated
34401A, and `RawPresenter` for the terminatorless Korads. That fix is real-hardware verified. This branch
had fixed the same finding independently, by reading replies through `ReplyCollector` (assemble
`Session.Output` chunks until a 300 ms quiet gap). The merge keeps both, and they compose:
- With `AsciiPresenter`, each line is already whole.
- `ReplyCollector` still guards the Korads, whose `RawPresenter` replies can split across reads too.

Later the same day, the rest of that Busylight custom-color UX note was dropped: the "Custom" radio option
and Enter acting as Apply. The resizable picker, remembered color and hex swatch that landed above were
judged good enough, and the item was removed from `BACKLOG.md`.

## Main windows: status bar, title state, styled output, Device-menu enabling, SCPI auto-detect progress

Both main windows (`TuiMode`, `MainWindow`) gained a connection-state UI that follows the connection:

- **Status bar/line.** WPF: a `StatusBar` with a colored dot and `Connected — tcp://…` /
  `Connecting — …` / `Disconnected — …` (green/amber/red). TUI: a full-width ` ● …` line under the
  send row, on the same colors (true-RGB `Scheme`).
- **Title.** ` — disconnected` is appended while the connection is closed
  (`ConnectionDescription.WindowTitle(..., connected)`). It used to keep describing a closed connection
  as live.
- **One refresh.** A single `RefreshConnectionUi` per window derives the File menu label, the send field,
  the title, the status bar and the Device menu items from `Session.State`. It runs at startup and after
  every connect, disconnect, self-disconnect and profile switch. While a connect is in flight, WPF shows
  "Connecting", which the transport never announces.
- **Styled output.** WPF output items are now `OutputLine(Text, Kind)` records: device output is normal,
  app status lines are dim italic, errors dark red. `ToString()` is the text, so string readers are
  unaffected. The TUI pane is one plain-text `Editor`, so it tags the lines instead: `[dev-term] …` /
  `[error] …`, the same shape as device lines' `[presenter]`.
- **Device menu enabling.** `DevicePanels.IsAvailable`:
  - K8055: HID `10CF:5500`–`5503`.
  - Busylight: HID `04D8:F848`, or any `27BB` device.
  - SCPI: any transport but HID.
  - All three are disabled while disconnected.
- **SCPI auto-detect.** The detect logic was duplicated between the TUI and WPF; it's now one
  `DevTerm.Devices.Scpi.ScpiAutoDetect`. Its wait is the new `CliOptions.ScpiAutoDetectTimeoutMs`
  (default 3000, validated 100–60000, saved in a profile only when changed, carried through the editor's
  Load→Connect), not a fixed 3 s. It reports progress (`Auto-detecting the SCPI instrument: sent *IDN?,
  waiting up to 3 s…`, plus a WPF wait cursor) and the outcome (matched / unrecognized reply / no reply).
- **The WPF "closing edge case" was already fixed.** It had been listed as open, as only worked around in
  test automation. A new test closes a really-shown, auto-connected `MainWindow` cleanly, and the spec and
  `WpfScreenshot` notes were corrected.

Bugs found along the way:
- The TUI's profile switch cleared the output `Editor`'s text but not its backing line list, so the
  previous connection's lines came back on the next append.
- `wpf-main-window.png`, captioned "disconnected", actually showed a connected window. Its screenshot
  test waited on a signal that became true early, and it rendered before a layout pass. Both are fixed,
  and the capture was regenerated and checked.
- WPF tests flaked about once every few full runs with a XAML parser race between STA threads, from
  the one test class (mine, earlier today) that ran in parallel. The WPF test assembly is now
  `DoNotParallelize`, and `StaTestRunner` keeps the original stack trace when it rethrows. See `CLAUDE.md`.

One TUI test (`TuiModeSwitchProfileTests.SwitchProfileAsync_SupersededByAnotherSwitchBeforeItResolves…`)
failed once in a full-suite run and then passed 18 consecutive reruns (12 of its classes together, 6 of the
full Console project). It wasn't reproduced, so the cause isn't known.

Tests: `MainWindowStateTests`, `ScpiAutoDetectTests`, `TuiModeConnectionStateTests` and
`MainWindowConnectionStateTests`, plus regenerated and inspected `tui-main-*`/`wpf-main-window*` captures.
Not verified against real hardware.

## Device control panels: collapsible sections, aligned labels, command preview, Notes, value constraints

Both generic control-panel renderers changed, for every `UiDefinition`-driven panel (K8055, Busylight,
SCPI): TUI `ControlPanelMode` and WPF `ControlPanelWindow`. See `docs/specs/device-control-panel.md`.

- **Collapsible sections.**
  - WPF: an `Expander` per labeled section, expanded by default.
  - TUI (Terminal.Gui v2.5.0 has no expander widget): a focusable `[-] Name`/`[+] Name` header `Button`
    that hides the section's rows. Rows are recomputed on every toggle, so a collapsed section leaves
    no gap.
  - Unlabeled sections have no header and are always shown.
- **Labels on one line, aligned per section.**
  - WPF: a per-section two-column `Grid` with an auto-sized, `NoWrap` label column. This fixes
    Busylight's "On (unit unconfirmed):" wrapping onto two lines.
  - TUI: the control column starts at the section's longest `Label:` + 1.
- **Notes at the bottom.** `UiDefinition.Description` (SCPI profiles' `Notes`) now renders as a
  collapsible, wrapped "Notes" section after the others, instead of one line at the top that the TUI cut
  off.
- **Command preview.**
  - A new optional `DevTerm.Core.Control.ICommandPreview.PreviewCommand(commandId, value)`, separate from
    `IControlSurface`, plus `CommandPreviewFormat`.
  - SCPI: `ScpiControlSurface` routes `InvokeAsync` and `PreviewCommand` through one `Resolve()`, and
    shows the terminator escaped (`MEAS:VOLT:DC? DEF\n`).
  - K8055: previews the HID report as hex, via a shared pure `Plan()`.
  - Busylight: previews Apply, the only command that sends.
  - WPF shows an "ⓘ" icon whose tooltip is recomputed on open. The TUI shows a `(i)` marker plus a
    `Sends: …` footer for the focused control, which follows its current value.
- **Values decoupled from widgets.**
  - A new serializable `DevTerm.UiDefinitions.ValueConstraint` (`Text`/`Integer`/`Number`,
    `Minimum?`/`Maximum?`, `ClampToRange`) on `TextFieldControl.Constraint`.
  - One shared `ValueValidator`, run by both renderers on commit and for parameter buttons. Invalid input
    is reported (TUI footer; WPF red status line) and not sent; valid numbers are normalized.
  - Slider/Numeric keep their Min/Max JSON and clamping. Unparsable input to them is now rejected instead
    of silently sending `DefaultValue`.
  - `ScpiParameterDefinition.Control` (`Numeric`/`Slider`/`Text`/`Choice`, optional) picks a SCPI
    parameter's widget independently of its `Kind`.

Verified by new unit tests (`ControlPanelModeLayoutTests`, `ControlPanelWindowLayoutTests`,
`ValueConstraintTests` with JSON and XML round-trips plus pre-constraint JSON, `ScpiCommandPreviewTests`,
`CommandPreviewFormatTests`, and K8055/Busylight preview tests). Every `*control-panel*` capture was
regenerated and inspected, plus a new `tui-control-panel-scpi-preview.png` (34401A, two sections collapsed,
`Sends: CONF:VOLT:DC DEF\n`). It was built in a parallel worktree and merged. Not verified against real
hardware; no wire-level behavior changed apart from rejecting invalid input before sending.

Limitations that remain are in the spec's Open items:
- TUI Notes don't re-wrap when the terminal is resized.
- Very long TUI rows can push `(i)` off-screen.
- Expand/collapse state isn't remembered between openings.

With all five sub-items landed, the "Device control panel UX polish" item was removed from `BACKLOG.md`.

## TUI output pane: per-line colors

The TUI output pane is a single `Terminal.Gui.Editor`, so it had no per-item styling like WPF's list.
Status and error lines could only be told apart by their `[dev-term]`/`[error]` tags.
`Terminal.Gui.Editor` turned out to be an AvalonEdit port with a real syntax-highlighting engine, so
`OutputHighlighting` installs a small XSHD definition (`Editor.HighlightingDefinition`) whose rules match
whole lines by tag:
- `[error] …` red and bold;
- `[dev-term] …` grey and italic;
- device lines in the default color.

The tags stay in the text for terminals without color. `OutputHighlightingTests` checks the colors on the
real rendered cells (`Driver.GetOutputBuffer()`), and `tui-main-*.png` were regenerated. This removes the
matching Open item from `docs/specs/tui-main-screen.md`.

Later still: the items in the three-phase UI batch moved from `BACKLOG.md` into `TODO.md` when work started, per this project's convention (in-progress work lives in `TODO.md`):
- the device-manifest editor and new display controls;
- the forms-from-one-definition consolidation;
- theming;
- logger mode and the logger-playback research item;
- the Stream Monitor capture/preview phase of the rendering-presenters item.

The real HPGL/PostScript/PCL rendering half of that item stays in `BACKLOG.md`.

## Stream Monitor (stream content detection, phase 1)

Built phase 1 of [docs/design/proposals/stream-content-detection.md](../design/proposals/stream-content-detection.md).
**Device > Stream Monitor...** in the TUI and WPF:
- watches the live session for images (PNG/JPEG/GIF/BMP/TIFF), HP-GL, PostScript and PCL;
- captures each one whole;
- auto-saves it to the connection's export folder (`~/.dev-term/exports` by default) as
  `{device}_{yyyyMMdd-HHmmss}.{ext}`.

WPF also previews native image formats and has Open Folder and Export As.... Spec:
[docs/specs/stream-monitor.md](../specs/stream-monitor.md); guide:
[docs/user-guide/stream-monitor.md](../user-guide/stream-monitor.md).

- **Detection** (`DevTerm.Core.StreamContent`, pure and unit-tested):
  - `StreamContentSniffer` matches signatures, including SCPI `#<n><len>` blocks.
  - `StreamContentEndFinder` finds structural ends: PNG `IEND`, JPEG EOI (by walking segments), the GIF
    trailer, BMP/EPS header sizes, `%%EOF`/Ctrl-D and the PJL UEL. Formats without one end after 2 s of
    silence, and there's a 64 MiB cap.
  - False-positive guards: HP-GL only counts at a reply boundary, and a lone `ESC E` isn't PCL.
  - `StreamContentWatcher` is an `IPresenter` that emits no text and raises `ContentDetected`. `Render`
    never throws. Timing goes through `TimeProvider`, so the tests use a fake clock.
- **Declared hint:** `ScpiCommandDefinition.ExpectedResponseFormat`. `ScpiControlSurface` calls
  `IStreamContentHintSink.ExpectResponse` on sinks in `Session.Presenters` before sending. The DG1062Z's
  `HCOPy:SDUMp:DATA?` declares `Image`.
- **Tap:** `StreamMonitor` (`DevTerm.Configuration`) binds a fresh watcher per session with
  `Session.AddPresenter`; the new `Session.RemovePresenter`/`Pipeline.RemovePresenter` unbind it.
  - It isn't a `--presenter`: it emits no text, and binding it in place lets monitoring be turned on and
    off mid-connection without touching any other presenter's output.
  - It follows profile switches (`SetSession`): a capture in progress on the old session is flushed and
    saved first.
  - Monitoring continues after the window closes (the TUI window is modal), and each capture adds a
    status line to the main output.
- **Not verified against real hardware yet** (DG1062Z screen capture, TDS2024 hardcopy); unit tests only.

Built in a parallel worktree and merged, with no conflicts. The full unit suite passes.

Still open:
- a CLI mode;
- a way for device manifests to declare a response format;
- more than one capture at a time;
- cleanup of old exports;
- phase 2 (HP-GL/PostScript/PCL preview and rasterizing).

## Device manifest panels, chart controls, and control-panel fit/memory

- **Live panels from device manifests.** A new **Device > Device Manifest...** item in both main windows
  is enabled whenever connected (`DevicePanel.Manifest`).
  - **Picker:** it lists `~/.dev-term/manifests`, then the app's `manifests\` folder
    (`InstalledManifests`/`ManifestCatalog`: folders with `device.json`, `.zip`, `.json`), or takes any path.
    It loads the choice with `DeviceManifestLoader` and opens it on the current session through the
    generic renderers.
  - **Parts** (`DevTerm.DeviceManifests.ManifestPanel`):
    - `ManifestUiBuilder`: the manifest's `Ui`, or one generated from its commands.
    - `ManifestControlSurface` (`IControlSurface` + `ICommandPreview`, the same shape as
      `ScpiControlSurface`): `{Name}`/`{value}` template substitution, numeric parse/clamp/`Format`,
      `Terminator`, query reply ids.
    - `ManifestReplyPresenter`: reply correlation, plus regex response patterns whose named groups
      publish live values. It renders no text and is bound only while the panel is open, via the new
      `Session.RemovePresenter`.
  - The SCPI presenter's line buffering and first-in-first-out reply correlation moved to
    `DevTerm.Core.Presenters.LineReplyPresenter`/`IReplyTracker`, shared by both.
  - New optional manifest fields: `Terminator`, `OutboundCommand.Id/IsQuery/ReplyId`,
    `CommandParameter.DefaultValue/Format`, `InboundProtocol.LineTerminated`. Manifest and UI JSON are now
    read case-insensitively.
  - First bundled manifest: **Loopback Sensor Demo**, driven by new loopback rules `MEAS?`/`Samples: N`
    (a deterministic simulated sensor per connection).
- **Chart controls.** `BarGraphControl`, `StripChartControl` and `VectorControl` (XY / XYZ / Polar, optional
  h/s/v color), plus `ChartChannel`, with JSON/XML round-trip tests.
  - Shared state lives in `LiveDisplayState`: parsing, clamping, history, scaling, polar conversion, trail
    and color.
  - `ChartPalette` gives a fixed 8-slot categorical color order.
  - TUI: `CellCharts`/`CellCanvasView` draw block-character bars and braille strip charts and vector plots,
    with per-cell color.
  - WPF: `LiveDisplayElements` draws them in `OnRender`.
- **TUI rows no longer run off the right edge.** The form measures its widest row after layout and scrolls
  sideways:
  - a horizontal scroll bar appears when needed;
  - focusing a control reveals it and its `(i)`, and focusing a section header scrolls back left;
  - Ctrl+PageUp/PageDown and a sideways mouse wheel also scroll.

  This was chosen over truncation because it covers every widget kind and hides nothing.
- **TUI Notes re-wrap on terminal resize**, after any layout pass that changes the visible width.
- **Section expand/collapse is remembered** per definition and section label for the life of the process,
  in both front ends (`SectionExpansionState`).

Tests: `ChartControlsTests`, `ManifestControlSurfaceTests` (FakeTransport wire bytes plus real loopback end
to end), `SectionExpansionStateTests`, `CellChartsTests`, `ControlPanelModeFitTests`,
`ManifestPanelWindowTests`, and a loopback sensor test. New captures: `tui-control-panel-manifest`,
`tui-control-panel-wide-row`, `wpf-control-panel-manifest`.

Built in a parallel worktree. On merge it conflicted with the Stream Monitor branch: both had added
`Session`/`Pipeline.RemovePresenter` with the same implementation and different doc comments (the comments
were combined), and both had added a Device menu item and matching spec/user-guide rows (both kept). The full
unit suite passes. Not verified against real hardware or by driving the real apps; headless/StaTestRunner and
loopback only.

Still open: the manifest editor (Phase 2 in `TODO.md`), auto-opening a profile's `ManifestName` panel,
binary `.ksy` decoding, chart hover/export, cleanup of extracted `.zip` temp folders, and sideways scrolling
in WPF.

## Logger mode and log playback (CLI, TUI, WPF)

- **New `DevTerm.Logging` project** (references only Core): a versioned JSON Lines session-log format, a
  header plus one record per event.
  - `tx`/`rx` store the raw chunk as base64, so playback is lossless, chunk boundaries included.
  - Records carry sequence numbers and monotonic UTC timestamps.
  - Unknown fields are ignored, unknown record types are kept verbatim, newer versions are refused, and a
    torn final line is tolerated.
  - See `docs/design/session-logging.md`.
- **`Session.AddObserver(ISessionObserver)`:** a passive tap on the session. It sees each received chunk
  before rendering, each send before the write, opens, and closes (a requested close versus a
  self-disconnect). It sits outside the pipeline, and an observer that throws is ignored. `SessionLogger`
  records through it and follows a live profile switch (writing a `session` record).
- **`PlaybackEngine`/`PlaybackController`:** pull-driven playback with an injectable `TimeProvider`.
  - Controls: 0.25x–10x and Max, step, rewind, seek, +10 s, and changing presenters.
  - Trim to a new log (`trimmedFrom`, original sequence numbers kept), and notes saved into the log.
  - `PlaybackPresenters` composes presenters with no transport (`AddDevTermPresenters`), so playback cannot
    reach a device.
- **CLI:** `--log <path>` or `--log true`, defaulting to
  `~/.dev-term/logs/{yyyyMMdd-HHmmss}_{profile|connection}.jsonl`. `--playback <log> [--presenter ..]
  [--playbackspeed n]` replays a log (0 = as fast as possible) and never connects.
- **TUI/WPF:** File > Start Logging... / Stop Logging (with `● REC` in the status line/bar) and File > Open Log
  for Playback.... The TUI's `PlaybackMode` is modal; WPF's `PlaybackWindow` is non-modal, with a seek slider
  and presenter check boxes. Specs: `docs/specs/playback-window.md`, `tui-main-screen.md`,
  `wpf-main-window.md`. User guide: `docs/user-guide/logging-and-playback.md`.
- **Verified** with the loopback transport and test fakes: the real CLI recorded a loopback session with
  `--log` and replayed it with `--playback`, including `--presenter hex --playbackspeed 4`. Not verified
  against real hardware yet.

Built in a parallel worktree. On merge it conflicted in `DevTerm.Configuration.csproj`, where this branch and
the panels branch each added a project reference (both kept), and in the TUI spec's status-line row (combined).
The full unit suite passes, 17 projects including the new 44-test `DevTerm.Logging.Tests`.

Known limits:
- A note can't be added to a log the same process is still recording.
- Logs load fully into memory, and backward seeks replay from the start.
- No skip-silence option, and notes can't be edited.
- The TUI has no seek slider.
- Quitting the TUI ends the log without a final `close` record; WPF records it.

With this, **Phase 1 of the UI batch is complete** (Stream Monitor, panels, logger/playback).

## Theming: light/dark/system and user themes (TUI + WPF)

- **Investigated first** (details in `docs/design/theming.md`):
  - **WPF Fluent `ThemeMode`: not used.** It works on net10.0-windows per window, but it's still experimental
    (WPF0001 on every use, which fails the build under TreatWarningsAsErrors). It re-templates every control
    with much larger metrics (a 60 px ListBox went from two rows to one). And Dark's window background is
    transparent (Mica), so RenderTargetBitmap screenshots come out white on white.
  - **Terminal.Gui v2.5.0's built-in themes: not used either.** There's no ConfigurationManager, and its
    Dark/Light themes leave the Base background as the terminal's own (`None`), so Dark is unreadable on a
    light terminal. `SchemeManager.AddScheme` does override a scheme live and process-wide, surviving
    Application.Init and ThemeManager switches.
- **New shared model in `DevTerm.Configuration`:**
  - `ThemeRole`: 29 semantic roles.
  - `DevTermTheme`: a WCAG contrast check and IsDark.
  - `BuiltInThemes`: light (the colors from before theming), dark, and `system` (via `AppsUseLightTheme` on
    Windows, `COLORFGBG` elsewhere).
  - `ThemeFile`: JSON user themes in `~/.dev-term/themes`, validated. Every problem is reported, and loading
    never throws.
  - `ThemeCatalog`, and `ActiveTheme` (the process-wide current theme and a `Changed` event).
- **Where the choice lives:** it's an app preference in the new `~/.dev-term/preferences.json`
  (`AppPreferencesStore`), never in a connection profile or `CliOptions`. `--theme` / `DEVTERM_THEME`
  override it for one run without saving. View > Theme in both front ends switches live and saves the choice.
- **WPF:** `WpfTheme` merges per-window role brushes (`DevTerm.<Role>`) and `SystemColors` overrides for
  non-stock themes. For dark themes, `DarkControls` supplies Button/ComboBox/MenuItem/ContextMenu templates at
  stock metrics. Charts re-render on a switch, and System follows a Windows app-mode change live.
- **TUI:** `TuiTheme` overrides Terminal.Gui's Base/Dialog/Accent/Menu/Error schemes with every role set
  explicitly; text fields get their own `fieldBackground`, since they have no border. The output XSHD is
  generated per theme, and the status line, Stream Monitor line and chart ink come from roles.
- **Hard-coded colors moved onto roles:** the status dot and line, REC, output/playback line styles, red
  error/hint texts, notes, info icons, swatch borders and the Stream Monitor. `ChartPalette` gains
  `DarkSlots`: the same eight hues in the same colorblind-safe order, stepped for a dark surface.
- **Tests:** Configuration `ThemeTests`, `TuiThemeTests` (live switch checked on rendered cells),
  `WpfThemeTests` (brushes on named elements, multi-window switch) and `ThemeScreenshotTests`. New Light/Dark
  screenshots are under `docs/user-guide/images/*-theme-*`. Docs: `docs/design/theming.md`, `docs/specs` (the
  TUI/WPF main screens' View > Theme, playback and Stream Monitor colors), `docs/user-guide/themes.md`.

Built in a parallel worktree, and it merged as a fast-forward. The full unit suite passes. Real launches with
`--theme dark` stayed up in both apps; not otherwise verified beyond screenshots and tests.

Still open:
- the TUI Connection Editor's "(not found)" hints aren't themed;
- there's no "follow the terminal's colors" TUI option (TUI System is resolved at startup);
- WPF check boxes, sliders, scroll bars and group-box borders keep their stock light chrome on dark (readable).

## Busylight: a "Custom" color radio (reported bug)

Reported: after choosing a preset color radio (Red, Green, ...), the custom color picked with **Custom...**
could no longer be applied again. The radios and the Custom... button share the `color` command, and nothing
selected the custom color short of reopening the picker. (Earlier today the "Custom radio" part of the
2026-09-23 note had been dropped as not needed; this reported bug is why it's back.)

The fix is generic, not Busylight-specific:
- New `ButtonControl.ColorPickerChoiceOption` names the option in the button's target `ChoiceControl` that
  stands for the picked color. `CustomColorChoices.Find` links the two.
- Both renderers keep them in step:
  - a pick selects that option without sending it again;
  - selecting that option sends the button's last picked `r,g,b` (`LastPickedColors`) instead of the option
    text, and opens the picker if no color has been picked yet.
- The Busylight's Color radios are now Red / Green / Blue / Yellow / **Custom** / Off, linked to its Custom...
  button.

Tests: `CustomColorChoicesTests` (linking, plus a JSON/XML round-trip of the new property), and
`BusylightCustomRadio_AfterAPreset_ReappliesTheRememberedCustomColor` in both front ends (Red, then Custom,
sends `Red`, then `10,20,30`). The picker-opening path is a modal dialog and isn't automated. The Busylight
captures were regenerated. Not verified against the real Busylight.
