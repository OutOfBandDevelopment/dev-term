<!-- Replacement styles/templates for the stock (Aero2) controls whose chrome is hard-coded light, so a
     dark theme doesn't end up with light-on-light text. Merged only for a dark theme (WpfTheme); every
     color is a DynamicResource theme role ("DevTerm.<Role>"), so the same templates follow a live
     switch between dark themes. Deliberately compact - same metrics as the stock controls (padding,
     arrow size), so a layout sized against the stock look still fits. See docs/design/theming.md. -->
<ResourceDictionary x:Class="DevTerm.Wpf.DarkControls"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style x:Key="DevTerm.FocusVisual">
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate>
          <Rectangle Margin="2" StrokeThickness="1" StrokeDashArray="1 2" Stroke="{DynamicResource DevTerm.Accent}" SnapsToDevicePixels="True" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Button: stock hover/pressed backgrounds are fixed light blues. -->
  <Style TargetType="Button">
    <Setter Property="FocusVisualStyle" Value="{StaticResource DevTerm.FocusVisual}" />
    <Setter Property="Background" Value="{DynamicResource DevTerm.ControlBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Padding" Value="1" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
            <ContentPresenter x:Name="contentPresenter" Margin="{TemplateBinding Padding}" Focusable="False" RecognizesAccessKey="True"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsDefaulted" Value="True">
              <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource DevTerm.Accent}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
              <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource DevTerm.Accent}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.SelectionBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.SelectionForeground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.MutedForeground}" />
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.Background}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Lists and menus: stock backgrounds are fixed white/#F0F0F0. -->
  <Style TargetType="ListBox">
    <Setter Property="Background" Value="{DynamicResource DevTerm.ControlBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
  </Style>

  <Style TargetType="TextBox">
    <Setter Property="Background" Value="{DynamicResource DevTerm.ControlBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
    <Setter Property="CaretBrush" Value="{DynamicResource DevTerm.ControlForeground}" />
  </Style>

  <Style TargetType="Menu">
    <Setter Property="Background" Value="{DynamicResource DevTerm.MenuBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.MenuForeground}" />
  </Style>

  <Style TargetType="StatusBar">
    <Setter Property="Background" Value="{DynamicResource DevTerm.MenuBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.MenuForeground}" />
  </Style>

  <Style TargetType="GroupBox">
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
  </Style>

  <Style TargetType="ToolTip">
    <Setter Property="Background" Value="{DynamicResource DevTerm.MenuBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.MenuForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
  </Style>

  <!-- MenuItem: the stock drop-down's popup background is a fixed light gray. One template for all
       four roles (top-level header/item, submenu header/item). -->
  <Style TargetType="MenuItem">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.MenuForeground}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="MenuItem">
          <Border x:Name="border" Background="Transparent" SnapsToDevicePixels="True">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="checkColumn" Width="22" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition x:Name="gestureColumn" Width="Auto" />
                <ColumnDefinition x:Name="arrowColumn" Width="14" />
              </Grid.ColumnDefinitions>
              <TextBlock x:Name="check" Grid.Column="0" Text="✓" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
              <ContentPresenter x:Name="header" Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" Margin="2,3,8,3" VerticalAlignment="Center" />
              <TextBlock x:Name="gesture" Grid.Column="2" Text="{TemplateBinding InputGestureText}" Margin="16,3,4,3" VerticalAlignment="Center"
                         Foreground="{DynamicResource DevTerm.MutedForeground}" />
              <Path x:Name="arrow" Grid.Column="3" Data="M 0 0 L 4 4 L 0 8 Z" Fill="{TemplateBinding Foreground}" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed" />
              <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False" Placement="Right" PopupAnimation="None"
                     IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}">
                <Border Background="{DynamicResource DevTerm.MenuBackground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}" BorderThickness="1" Padding="2">
                  <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" KeyboardNavigation.TabNavigation="Cycle" Grid.IsSharedSizeScope="True" />
                  </ScrollViewer>
                </Border>
              </Popup>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
              <Setter TargetName="checkColumn" Property="Width" Value="0" />
              <Setter TargetName="arrowColumn" Property="Width" Value="0" />
              <Setter TargetName="gestureColumn" Property="Width" Value="0" />
              <Setter TargetName="header" Property="Margin" Value="6,2,6,2" />
              <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom" />
            </Trigger>
            <Trigger Property="Role" Value="TopLevelItem">
              <Setter TargetName="checkColumn" Property="Width" Value="0" />
              <Setter TargetName="arrowColumn" Property="Width" Value="0" />
              <Setter TargetName="gestureColumn" Property="Width" Value="0" />
              <Setter TargetName="header" Property="Margin" Value="6,2,6,2" />
            </Trigger>
            <Trigger Property="Role" Value="SubmenuHeader">
              <Setter TargetName="arrow" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="check" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.SelectionBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.SelectionForeground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.MutedForeground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Separator">
          <Border Height="1" Margin="22,3,2,3" Background="{DynamicResource DevTerm.ControlBorder}" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ContextMenu">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.MenuForeground}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ContextMenu">
          <Border Background="{DynamicResource DevTerm.MenuBackground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}" BorderThickness="1" Padding="2">
            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ComboBox: the stock toggle-button chrome and editable text area are fixed light. -->
  <ControlTemplate x:Key="DevTerm.ComboBoxToggle" TargetType="ToggleButton">
    <Border x:Name="border" Background="{DynamicResource DevTerm.ControlBackground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}" BorderThickness="1" SnapsToDevicePixels="True">
      <Path x:Name="arrow" Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource DevTerm.ControlForeground}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0" />
    </Border>
    <ControlTemplate.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource DevTerm.Accent}" />
      </Trigger>
      <Trigger Property="IsChecked" Value="True">
        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource DevTerm.Accent}" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.Background}" />
        <Setter TargetName="arrow" Property="Fill" Value="{DynamicResource DevTerm.MutedForeground}" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>

  <Style TargetType="ComboBox">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="Background" Value="{DynamicResource DevTerm.ControlBackground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
    <Setter Property="Padding" Value="6,3,5,3" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid SnapsToDevicePixels="True">
            <ToggleButton x:Name="toggle" Template="{StaticResource DevTerm.ComboBoxToggle}" Focusable="False" ClickMode="Press"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
            <ContentPresenter x:Name="contentPresenter" IsHitTestVisible="False" Margin="{TemplateBinding Padding}" Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            <TextBox x:Name="PART_EditableTextBox" Visibility="Collapsed" Margin="1,1,18,1" Padding="3,2,0,2" BorderThickness="0"
                     Background="{DynamicResource DevTerm.ControlBackground}" Foreground="{TemplateBinding Foreground}" CaretBrush="{TemplateBinding Foreground}"
                     IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}}"
                     HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
            <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False" Placement="Bottom" PopupAnimation="None"
                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
              <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="{TemplateBinding MaxDropDownHeight}"
                      Background="{DynamicResource DevTerm.ControlBackground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}" BorderThickness="1">
                <ScrollViewer>
                  <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                </ScrollViewer>
              </Border>
            </Popup>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsEditable" Value="True">
              <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
              <Setter TargetName="contentPresenter" Property="Visibility" Value="Hidden" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.MutedForeground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ComboBoxItem">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBoxItem">
          <Border x:Name="border" Background="Transparent" Padding="4,2,4,2" SnapsToDevicePixels="True">
            <ContentPresenter />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.SelectionBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.SelectionForeground}" />
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
