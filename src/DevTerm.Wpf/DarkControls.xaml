<!-- Replacement styles/templates for the stock (Aero2) controls whose chrome is hard-coded light, so a
     dark theme doesn't end up with light-on-light text. Merged only for a dark theme (WpfTheme); every
     color is a DynamicResource theme role ("DevTerm.<Role>"), so the same templates follow a live
     switch between dark themes. Deliberately compact - same metrics as the stock controls (padding,
     arrow size), so a layout sized against the stock look still fits. See docs/design/theming.md. -->
<ResourceDictionary x:Class="DevTerm.Wpf.DarkControls"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style x:Key="DevTerm.FocusVisual">
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate>
          <Rectangle Margin="2" StrokeThickness="1" StrokeDashArray="1 2" Stroke="{DynamicResource DevTerm.Accent}" SnapsToDevicePixels="True" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Button: stock hover/pressed backgrounds are fixed light blues. -->
  <Style TargetType="Button">
    <Setter Property="FocusVisualStyle" Value="{StaticResource DevTerm.FocusVisual}" />
    <Setter Property="Background" Value="{DynamicResource DevTerm.ControlBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Padding" Value="1" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
            <ContentPresenter x:Name="contentPresenter" Margin="{TemplateBinding Padding}" Focusable="False" RecognizesAccessKey="True"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsDefaulted" Value="True">
              <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource DevTerm.Accent}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
              <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource DevTerm.Accent}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.SelectionBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.SelectionForeground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.MutedForeground}" />
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.Background}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Lists and menus: stock backgrounds are fixed white/#F0F0F0. -->
  <Style TargetType="ListBox">
    <Setter Property="Background" Value="{DynamicResource DevTerm.ControlBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
  </Style>

  <!-- ListBoxItem: the stock hover/selection fills and frames are fixed light blues/grays (an
       unfocused list's selected row gets a #DADADA frame). -->
  <Style TargetType="ListBoxItem">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="Padding" Value="4,1" />
    <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
    <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListBoxItem">
          <Border x:Name="Bd" Background="Transparent" BorderThickness="1" BorderBrush="Transparent" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="False" />
                <Condition Property="IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
              <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="True" />
                <Condition Property="IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource DevTerm.SelectionBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.SelectionForeground}" />
            </MultiTrigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.MutedForeground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="TextBox">
    <Setter Property="Background" Value="{DynamicResource DevTerm.ControlBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
    <Setter Property="CaretBrush" Value="{DynamicResource DevTerm.ControlForeground}" />
  </Style>

  <Style TargetType="Menu">
    <Setter Property="Background" Value="{DynamicResource DevTerm.MenuBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.MenuForeground}" />
  </Style>

  <Style TargetType="StatusBar">
    <Setter Property="Background" Value="{DynamicResource DevTerm.MenuBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.MenuForeground}" />
  </Style>

  <!-- GroupBox: the stock template draws a hard-coded white inner line inside its border (a doubled
       white frame on a dark window). Header over the frame's top edge, like the stock look. -->
  <Style TargetType="GroupBox">
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="6,2,6,6" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="GroupBox">
          <Grid SnapsToDevicePixels="True">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Grid.RowSpan="2" Margin="0,8,0,0" CornerRadius="3"
                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" />
            <Border Grid.Row="0" HorizontalAlignment="Left" Margin="6,0,0,0" Padding="3,0" Background="{DynamicResource DevTerm.Background}">
              <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" />
            </Border>
            <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ToolTip">
    <Setter Property="Background" Value="{DynamicResource DevTerm.MenuBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.MenuForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
  </Style>

  <!-- MenuItem: the stock drop-down's popup background is a fixed light gray. One template for all
       four roles (top-level header/item, submenu header/item). -->
  <Style TargetType="MenuItem">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.MenuForeground}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="MenuItem">
          <Border x:Name="border" Background="Transparent" SnapsToDevicePixels="True">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="checkColumn" Width="22" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition x:Name="gestureColumn" Width="Auto" />
                <ColumnDefinition x:Name="arrowColumn" Width="14" />
              </Grid.ColumnDefinitions>
              <TextBlock x:Name="check" Grid.Column="0" Text="✓" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
              <ContentPresenter x:Name="header" Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" Margin="2,3,8,3" VerticalAlignment="Center" />
              <TextBlock x:Name="gesture" Grid.Column="2" Text="{TemplateBinding InputGestureText}" Margin="16,3,4,3" VerticalAlignment="Center"
                         Foreground="{DynamicResource DevTerm.MutedForeground}" />
              <Path x:Name="arrow" Grid.Column="3" Data="M 0 0 L 4 4 L 0 8 Z" Fill="{TemplateBinding Foreground}" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed" />
              <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False" Placement="Right" PopupAnimation="None"
                     IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}">
                <Border Background="{DynamicResource DevTerm.MenuBackground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}" BorderThickness="1" Padding="2">
                  <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" KeyboardNavigation.TabNavigation="Cycle" Grid.IsSharedSizeScope="True" />
                  </ScrollViewer>
                </Border>
              </Popup>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
              <Setter TargetName="checkColumn" Property="Width" Value="0" />
              <Setter TargetName="arrowColumn" Property="Width" Value="0" />
              <Setter TargetName="gestureColumn" Property="Width" Value="0" />
              <Setter TargetName="header" Property="Margin" Value="6,2,6,2" />
              <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom" />
            </Trigger>
            <Trigger Property="Role" Value="TopLevelItem">
              <Setter TargetName="checkColumn" Property="Width" Value="0" />
              <Setter TargetName="arrowColumn" Property="Width" Value="0" />
              <Setter TargetName="gestureColumn" Property="Width" Value="0" />
              <Setter TargetName="header" Property="Margin" Value="6,2,6,2" />
            </Trigger>
            <Trigger Property="Role" Value="SubmenuHeader">
              <Setter TargetName="arrow" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="check" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.SelectionBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.SelectionForeground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.MutedForeground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Separator">
          <Border Height="1" Margin="22,3,2,3" Background="{DynamicResource DevTerm.ControlBorder}" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ContextMenu">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.MenuForeground}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ContextMenu">
          <Border Background="{DynamicResource DevTerm.MenuBackground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}" BorderThickness="1" Padding="2">
            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ComboBox: the stock toggle-button chrome and editable text area are fixed light. -->
  <ControlTemplate x:Key="DevTerm.ComboBoxToggle" TargetType="ToggleButton">
    <Border x:Name="border" Background="{DynamicResource DevTerm.ControlBackground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}" BorderThickness="1" SnapsToDevicePixels="True">
      <Path x:Name="arrow" Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource DevTerm.ControlForeground}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0" />
    </Border>
    <ControlTemplate.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource DevTerm.Accent}" />
      </Trigger>
      <Trigger Property="IsChecked" Value="True">
        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource DevTerm.Accent}" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.Background}" />
        <Setter TargetName="arrow" Property="Fill" Value="{DynamicResource DevTerm.MutedForeground}" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>

  <Style TargetType="ComboBox">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="Background" Value="{DynamicResource DevTerm.ControlBackground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource DevTerm.ControlBorder}" />
    <Setter Property="Padding" Value="6,3,5,3" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid SnapsToDevicePixels="True">
            <ToggleButton x:Name="toggle" Template="{StaticResource DevTerm.ComboBoxToggle}" Focusable="False" ClickMode="Press"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
            <ContentPresenter x:Name="contentPresenter" IsHitTestVisible="False" Margin="{TemplateBinding Padding}" Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            <TextBox x:Name="PART_EditableTextBox" Visibility="Collapsed" Margin="1,1,18,1" Padding="3,2,0,2" BorderThickness="0"
                     Background="{DynamicResource DevTerm.ControlBackground}" Foreground="{TemplateBinding Foreground}" CaretBrush="{TemplateBinding Foreground}"
                     IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}}"
                     HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
            <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False" Placement="Bottom" PopupAnimation="None"
                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
              <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="{TemplateBinding MaxDropDownHeight}"
                      Background="{DynamicResource DevTerm.ControlBackground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}" BorderThickness="1">
                <ScrollViewer>
                  <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                </ScrollViewer>
              </Border>
            </Popup>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsEditable" Value="True">
              <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
              <Setter TargetName="contentPresenter" Property="Visibility" Value="Hidden" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.MutedForeground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ScrollBar: the stock track and thumb are fixed #F0F0F0/#CDCDCD - a light stripe down every
       list, form and panel. Stock metrics: 17px wide, arrow buttons at each end. -->
  <Style x:Key="DevTerm.ScrollBarArrow" TargetType="RepeatButton">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RepeatButton">
          <Border x:Name="border" Background="{DynamicResource DevTerm.Background}">
            <Path x:Name="arrow" Data="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Fill="{DynamicResource DevTerm.MutedForeground}"
                  HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="arrow" Property="Fill" Value="{DynamicResource DevTerm.Accent}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="DevTerm.ScrollBarPage" TargetType="RepeatButton">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RepeatButton">
          <Border Background="Transparent" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="DevTerm.ScrollBarThumb" TargetType="Thumb">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Thumb">
          <Border x:Name="thumb" Background="{DynamicResource DevTerm.ControlBorder}" Margin="3" CornerRadius="2" />
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="thumb" Property="Background" Value="{DynamicResource DevTerm.MutedForeground}" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
              <Setter TargetName="thumb" Property="Background" Value="{DynamicResource DevTerm.Accent}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <ControlTemplate x:Key="DevTerm.VerticalScrollBar" TargetType="ScrollBar">
    <Grid Background="{DynamicResource DevTerm.Background}" SnapsToDevicePixels="True">
      <Grid.RowDefinitions>
        <RowDefinition Height="17" />
        <RowDefinition Height="*" />
        <RowDefinition Height="17" />
      </Grid.RowDefinitions>
      <RepeatButton Grid.Row="0" Style="{StaticResource DevTerm.ScrollBarArrow}" Command="ScrollBar.LineUpCommand" Tag="M 0 4 L 4 0 L 8 4 Z" />
      <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="True">
        <Track.DecreaseRepeatButton>
          <RepeatButton Style="{StaticResource DevTerm.ScrollBarPage}" Command="ScrollBar.PageUpCommand" />
        </Track.DecreaseRepeatButton>
        <Track.Thumb>
          <Thumb Style="{StaticResource DevTerm.ScrollBarThumb}" />
        </Track.Thumb>
        <Track.IncreaseRepeatButton>
          <RepeatButton Style="{StaticResource DevTerm.ScrollBarPage}" Command="ScrollBar.PageDownCommand" />
        </Track.IncreaseRepeatButton>
      </Track>
      <RepeatButton Grid.Row="2" Style="{StaticResource DevTerm.ScrollBarArrow}" Command="ScrollBar.LineDownCommand" Tag="M 0 0 L 4 4 L 8 0 Z" />
    </Grid>
  </ControlTemplate>

  <ControlTemplate x:Key="DevTerm.HorizontalScrollBar" TargetType="ScrollBar">
    <Grid Background="{DynamicResource DevTerm.Background}" SnapsToDevicePixels="True">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="17" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="17" />
      </Grid.ColumnDefinitions>
      <RepeatButton Grid.Column="0" Style="{StaticResource DevTerm.ScrollBarArrow}" Command="ScrollBar.LineLeftCommand" Tag="M 4 0 L 0 4 L 4 8 Z" />
      <Track x:Name="PART_Track" Grid.Column="1">
        <Track.DecreaseRepeatButton>
          <RepeatButton Style="{StaticResource DevTerm.ScrollBarPage}" Command="ScrollBar.PageLeftCommand" />
        </Track.DecreaseRepeatButton>
        <Track.Thumb>
          <Thumb Style="{StaticResource DevTerm.ScrollBarThumb}" />
        </Track.Thumb>
        <Track.IncreaseRepeatButton>
          <RepeatButton Style="{StaticResource DevTerm.ScrollBarPage}" Command="ScrollBar.PageRightCommand" />
        </Track.IncreaseRepeatButton>
      </Track>
      <RepeatButton Grid.Column="2" Style="{StaticResource DevTerm.ScrollBarArrow}" Command="ScrollBar.LineRightCommand" Tag="M 0 0 L 4 4 L 0 8 Z" />
    </Grid>
  </ControlTemplate>

  <Style TargetType="ScrollBar">
    <Setter Property="Width" Value="17" />
    <Setter Property="MinWidth" Value="17" />
    <Setter Property="Template" Value="{StaticResource DevTerm.VerticalScrollBar}" />
    <Style.Triggers>
      <Trigger Property="Orientation" Value="Horizontal">
        <Setter Property="Width" Value="Auto" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="Height" Value="17" />
        <Setter Property="MinHeight" Value="17" />
        <Setter Property="Template" Value="{StaticResource DevTerm.HorizontalScrollBar}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- The corner square between two scroll bars is a stock ScrollViewer part painted SystemColors.Control - already themed. -->

  <!-- CheckBox/RadioButton: the stock box/circle is fixed white with a dark glyph. -->
  <Style TargetType="CheckBox">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.Foreground}" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DevTerm.FocusVisual}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <Grid Background="Transparent" SnapsToDevicePixels="True">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border x:Name="box" Width="14" Height="14" VerticalAlignment="Center" BorderThickness="1"
                    Background="{DynamicResource DevTerm.ControlBackground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}">
              <Grid>
                <Path x:Name="mark" Data="M 0 4 L 3 7 L 9 0" Stretch="Uniform" Stroke="{DynamicResource DevTerm.ControlForeground}" StrokeThickness="2" Margin="1.5" Visibility="Collapsed" />
                <Rectangle x:Name="indeterminate" Fill="{DynamicResource DevTerm.ControlForeground}" Margin="3" Visibility="Collapsed" />
              </Grid>
            </Border>
            <ContentPresenter x:Name="content" Grid.Column="1" Margin="4,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True" Focusable="False" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="HasContent" Value="False">
              <Setter TargetName="content" Property="Margin" Value="0" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="mark" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsChecked" Value="{x:Null}">
              <Setter TargetName="indeterminate" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource DevTerm.Accent}" />
              <Setter TargetName="box" Property="Background" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.MutedForeground}" />
              <Setter TargetName="mark" Property="Stroke" Value="{DynamicResource DevTerm.MutedForeground}" />
              <Setter TargetName="box" Property="Background" Value="{DynamicResource DevTerm.Background}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="RadioButton">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.Foreground}" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DevTerm.FocusVisual}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RadioButton">
          <Grid Background="Transparent" SnapsToDevicePixels="True">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Width="14" Height="14" VerticalAlignment="Center">
              <Ellipse x:Name="circle" Fill="{DynamicResource DevTerm.ControlBackground}" Stroke="{DynamicResource DevTerm.ControlBorder}" StrokeThickness="1" />
              <Ellipse x:Name="dot" Fill="{DynamicResource DevTerm.ControlForeground}" Margin="4" Visibility="Collapsed" />
            </Grid>
            <ContentPresenter x:Name="content" Grid.Column="1" Margin="4,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True" Focusable="False" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="HasContent" Value="False">
              <Setter TargetName="content" Property="Margin" Value="0" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="dot" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="circle" Property="Stroke" Value="{DynamicResource DevTerm.Accent}" />
              <Setter TargetName="circle" Property="Fill" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.MutedForeground}" />
              <Setter TargetName="dot" Property="Fill" Value="{DynamicResource DevTerm.MutedForeground}" />
              <Setter TargetName="circle" Property="Fill" Value="{DynamicResource DevTerm.Background}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Expander (downward only - the only direction dev-term uses): the stock header's circle is fixed white. -->
  <Style TargetType="Expander">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.Foreground}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Expander">
          <DockPanel SnapsToDevicePixels="True">
            <ToggleButton x:Name="header" DockPanel.Dock="Top" Focusable="True" FocusVisualStyle="{StaticResource DevTerm.FocusVisual}"
                          IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                          Foreground="{TemplateBinding Foreground}" HorizontalContentAlignment="Left"
                          Content="{TemplateBinding Header}" ContentTemplate="{TemplateBinding HeaderTemplate}">
              <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                  <Grid Background="Transparent">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="19" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Ellipse x:Name="circle" Width="19" Height="19" Fill="{DynamicResource DevTerm.ControlBackground}" Stroke="{DynamicResource DevTerm.ControlBorder}" />
                    <Path x:Name="arrow" Data="M 1 1.5 L 4.5 5 L 8 1.5" Stroke="{DynamicResource DevTerm.ControlForeground}" StrokeThickness="2"
                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <ContentPresenter Grid.Column="1" Margin="4,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True" />
                  </Grid>
                  <ControlTemplate.Triggers>
                    <Trigger Property="IsChecked" Value="True">
                      <Setter TargetName="arrow" Property="Data" Value="M 1 5 L 4.5 1.5 L 8 5" />
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="True">
                      <Setter TargetName="circle" Property="Stroke" Value="{DynamicResource DevTerm.Accent}" />
                    </Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate>
              </ToggleButton.Template>
            </ToggleButton>
            <ContentPresenter x:Name="content" Visibility="Collapsed" Margin="{TemplateBinding Padding}" />
          </DockPanel>
          <ControlTemplate.Triggers>
            <Trigger Property="IsExpanded" Value="True">
              <Setter TargetName="content" Property="Visibility" Value="Visible" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Slider (horizontal - the only orientation dev-term uses): the stock track and thumb are fixed light. -->
  <Style x:Key="DevTerm.SliderThumb" TargetType="Thumb">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Thumb">
          <Border x:Name="thumb" Width="11" Height="18" CornerRadius="2" BorderThickness="1"
                  Background="{DynamicResource DevTerm.ControlForeground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}" />
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="thumb" Property="Background" Value="{DynamicResource DevTerm.Accent}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="thumb" Property="Background" Value="{DynamicResource DevTerm.MutedForeground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="Slider">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Slider">
          <Grid MinHeight="{TemplateBinding MinHeight}" Background="Transparent" SnapsToDevicePixels="True">
            <Border Height="4" Margin="5,0" VerticalAlignment="Center" CornerRadius="2"
                    Background="{DynamicResource DevTerm.ControlBackground}" BorderBrush="{DynamicResource DevTerm.ControlBorder}" BorderThickness="1" />
            <Track x:Name="PART_Track">
              <Track.DecreaseRepeatButton>
                <RepeatButton Style="{StaticResource DevTerm.ScrollBarPage}" Command="Slider.DecreaseLarge" />
              </Track.DecreaseRepeatButton>
              <Track.Thumb>
                <Thumb Style="{StaticResource DevTerm.SliderThumb}" VerticalAlignment="Center" />
              </Track.Thumb>
              <Track.IncreaseRepeatButton>
                <RepeatButton Style="{StaticResource DevTerm.ScrollBarPage}" Command="Slider.IncreaseLarge" />
              </Track.IncreaseRepeatButton>
            </Track>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ComboBoxItem">
    <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBoxItem">
          <Border x:Name="border" Background="Transparent" Padding="4,2,4,2" SnapsToDevicePixels="True">
            <ContentPresenter />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.SelectionBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.SelectionForeground}" />
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter TargetName="border" Property="Background" Value="{DynamicResource DevTerm.ControlHoverBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource DevTerm.ControlForeground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
