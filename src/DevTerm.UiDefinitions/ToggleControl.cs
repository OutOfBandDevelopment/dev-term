namespace DevTerm.UiDefinitions;

/// <summary>A boolean control (e.g. "Mute", a digital output channel).</summary>
public sealed class ToggleControl : UiControl
{
    public bool DefaultValue { get; set; }
}
